test_that("plotting a parallel coordinate plot works", {

  skip("skipping") # skip test for now
  path <- file.path(test_object_path("experiment"), "experiment.RDS")
  e <- readRDS(file = path)


})
